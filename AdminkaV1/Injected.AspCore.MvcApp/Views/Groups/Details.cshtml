@model Group

<h3>Group Details (@Model.GroupId)</h3>

<div>
    <hr/>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.GroupName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GroupName)
        </dd>

        <dt>
            AD Group
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GroupAdName)
        </dd>

        <dt>
            Privileges
        </dt>

        <dd>
            @{  var privileges = Model.GetPrivileges();
                foreach (var p in privileges)
                {
                    <span class="dashboardcode-adminka-item">@Html.ActionLink(p.PrivilegeName, "Details", "Privileges", new { id = p.PrivilegeId }, null)</span>
                }
            }
        </dd>

        <dt>
            Roles
        </dt>

        <dd>
            @{  var roles = Model.GetRoles();
                foreach (var r in roles)
                {
                    <span class="dashboardcode-adminka-item">@Html.ActionLink(r.RoleName, "Details", "Roles", new { id = r.RoleId }, null)</span>
                }
            }
        </dd>

        <dt>
            Users
        </dt>

        <dd>
            @{  var users = Model.GetUsers();
                foreach (var u in users)
                {
                    <span class="dashboardcode-adminka-item">@Html.ActionLink(u.LoginName, "Details", "Users", new { id = u.UserId }, null)</span>
                }
            }
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Back to List", "Index")
</p>
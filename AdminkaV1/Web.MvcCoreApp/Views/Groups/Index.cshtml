@model IEnumerable<Group>
@{
    var UserContext = (UserContext)ViewBag.UserContext;
}
<h3>Groups</h3>

<table id="adminka-groups-table" class="table table-hover table-bordered mainTable">
    <thead>
        <tr>
            <th>
            @if (UserContext.HasPrivilege(Privilege.ConfigureSystem))
            {
                <button type="button" class="btn btn-secondary btn-sm" onclick="location.href='@Url.Action("Create", "Groups")'"><span class="glyphicon glyphicon-plus-sign"></span></button>
            }
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupAdName)
            </th>
            <th>
                Privileges
            </th>
            <th>
                Roles
            </th>
            <th>
                Users
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="tableActionColumn">
                    <button type="button" class="btn btn-secondary btn-sm" onclick="location.href='@Url.Action("Details", "Groups", new { id = item.GroupId })'"><span class="glyphicon  glyphicon-list"></span></button>
                    @if (UserContext.HasPrivilege(Privilege.ConfigureSystem))
                    {
                        <button type="button" class="btn btn-secondary btn-sm" onclick="location.href='@Url.Action("Edit", "Groups", new { id = item.GroupId })'"><span class="glyphicon  glyphicon-edit"></span></button>
                        <button type="button" class="btn btn-secondary btn-sm" onclick="location.href='@Url.Action("Delete", "Groups", new { id = item.GroupId })'"><span class="glyphicon  glyphicon-remove"></span></button>
                    }
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GroupId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GroupName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GroupAdName)
                </td>
                <td>
                    @{  var privileges = item.GetPrivileges();
                        foreach (var p in privileges)
                        {
                            <div><a class="dashboardcode-adminka-item" href="@Url.Action("Details", "Privileges", new { id = p.PrivilegeId })">@p.PrivilegeName</a></div>
                        }
                    }
                </td>
                <td>
                    @{  var roles = item.GetRoles();
                        foreach (var r in roles)
                        {
                        <div><a class="dashboardcode-adminka-item" href="@Url.Action("Details", "Roles", new { id = r.RoleId })">@r.RoleName</a></div>
                        }
                     }
                </td>
                <td>
                    @{  var users = item.GetUsers();
                        @foreach (var u in users)
                        {
                            <div><a class="dashboardcode-adminka-item" href="@Url.Action("Details", "Users", new { id = u.UserId })">@u.LoginName</a></div>
                        }
                    }
                </td>
            </tr>
        }
    </tbody>
</table>